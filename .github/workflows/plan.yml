name: Terraform Plan

on:
  pull_request:
  push:
    branches: [main]

jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t infractl .
      - run: docker run infractl plan --env dev
        env:
          CI: "true"
